name: getusermessages CI

on:
  pull_request:
    branches: [ development ]
    paths:
    - 'backend/getusermessages/**'
    
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14.x
      - run: npm ci
        working-directory: backend/getusermessages
      - run: npm test
        working-directory: backend/getusermessages
