name: Node.js CI

on:
  pull_request:
    branches: [ development ]
    paths:
      - 'backend/onconnect/**'

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x, 14.x, 16.x]

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14.x
      - run: npm ci
        working-directory: backend/onconnect
      - run: npm test
        working-directory: backend/onconnect
