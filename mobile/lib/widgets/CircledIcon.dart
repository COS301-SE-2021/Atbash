import 'package:flutter/material.dart';

class CircledIcon extends StatelessWidget {
  const CircledIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
